TODO
----

Report fails back to SBo maintainers

Fix 'remove' argument parsing (no pkg, sb exists)

After building, don't remove deps that were already installed

Test already-built packages

Install already-built packages

get deps from other repos (using slackpkg?)

Queuefiles (factorial deps)

More testing for multilib cross-compiles

ARM support

Archive/restore upgraded/removed packages and source

speed up gen_repos

do a FAQ

progress bar/spinner/'ok'/whatever

Install/uninstall acceleration

Rebuilds to permit circular deps

logrotate

share package dirs

sqlite database for package revisions?

sqlite database for expected build times?
