if ! getent group wildfly | grep -q ^wildfly: 2>/dev/null ; then
  groupadd -g 281 wildfly
fi

if ! getent passwd wildfly | grep -q ^wildfly: 2>/dev/null ; then
  useradd -u 281 -M -r -s /bin/bash -d /usr/share/wildfly -c "Wildfly Application Server" -g wildfly wildfly
fi
